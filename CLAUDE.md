# CLAUDE.md

이 저장소에서 작업하는 AI(Claude)를 위한 가이드입니다.

## 프로젝트가 무엇인가

- **윤이(아이)와 아빠가 AI와 함께 만드는 횡스크롤 액션 게임.** 최종 사용자는 어린이입니다.
- iPad Safari(홈 화면 추가 = 전체화면 웹앱)와 데스크톱 브라우저에서 플레이합니다.
- GitHub Pages(main 브랜치 root)로 배포됩니다. push하면 곧 서비스에 반영되므로
  **동작이 검증된 코드만 커밋**합니다.

## 절대 규칙

1. **빌드 도구 금지.** 번들러·트랜스파일러·npm 의존성을 쓰지 않습니다. `index.html`이
   `<script src>`로 `js/*.js`를 순서대로 읽는 게 전부입니다.
   - 예외: `tools/`의 Node 스크립트. 이건 **빌드가 아니라 한 번 돌리고 마는 변환기**이고,
     결과물을 저장소에 그대로 커밋하므로 게임 실행에는 Node가 필요 없습니다.
2. **어린이에게 적합한 내용만.** 폭력적·무서운 표현 금지. 실총기·유혈 표현 금지.
   패배 메시지도 다정하게("다시 도전! 💪"). UI 문구는 한국어, 쉬운 단어로.
3. **저작권 있는 자산 금지.** 아래 "자산 정책"을 따릅니다.
4. **오프라인 동작.** 네트워크 요청 없이 페이지 로드만으로 완전히 동작해야 합니다.
   외부 CDN을 쓰지 않습니다.

## 자산 정책

**쓸 수 있는 것**
- **CC0(퍼블릭 도메인) 자산** — [Kenney](https://kenney.nl/) 등. `assets/`에 두고
  출처와 라이선스 파일([assets/KENNEY-LICENSE.txt](assets/KENNEY-LICENSE.txt))을 함께 커밋합니다.
- 캔버스·SVG·이모지로 **직접 그린 것**.
- WebAudio로 **직접 합성한 소리**.

**쓸 수 없는 것**
- **닌텐도를 비롯한 상용 게임의 스프라이트·음악.** 마리오 클론 저장소들 상당수가
  원본 에셋을 그대로 담고 있습니다. 코드 구조를 눈으로 참고하는 건 괜찮지만
  **파일을 복사해 오지 않습니다.** 이 저장소는 공개(public)입니다.
- 라이선스가 불분명하거나 GPL 같은 전파성 라이선스의 코드. 가져오기 전에 반드시 확인하고,
  확인이 안 되면 가져오지 않습니다.

**주인공은 직접 그립니다.** Kenney 팩에도 플레이어 스프라이트가 있지만, 주인공은
윤이가 고른 캐릭터라 [js/anim.js](js/anim.js)에서 캔버스로 직접 그립니다. Kenney는
배경·타일·아이템처럼 **누가 그려도 되는 부분**에만 씁니다.

## 스테이지 고치는 법

스테이지는 [Tiled](https://www.mapeditor.org/)로 편집합니다. 좌표를 손으로 박지 않습니다.

```bash
# 1) Tiled로 stage/stage1.tmj 를 열어 편집하고 저장
# 2) 게임이 읽는 데이터로 변환
node tools/tiled-to-stage.js
```

- 레이어 네 개: `plats`(사각형) · `blocks`(사각형) · `enemies`(점) · `items`(점)
- 적·아이템은 오브젝트의 `kind` 속성으로 종류를 정합니다
- 맵 속성으로 `width` · `bossAt` · `arenaL` · `arenaR` 을 지정합니다
- 변환기가 **모르는 종류나 범위를 벗어난 값을 막아줍니다**. 통과하지 못하면 커밋하지 않습니다
- `js/stage-data.js`는 **생성물이라 손으로 고치지 않습니다**
- 반대 방향(데이터 → Tiled 파일)이 필요하면 `node tools/stage-to-tiled.js`

## 아이콘

`icon.png`는 생성물입니다. 손으로 고치지 말고 `node tools/make-icon.js` 를 다시 돌리세요.
iPad에서 "홈 화면에 추가"할 때 쓰이므로 없으면 빈 아이콘이 나옵니다.

## 코드 구조

`index.html`의 `<script>` 순서가 곧 의존 순서입니다. 전역을 그대로 공유하는 구조라
모듈 시스템이 없습니다. 각 파일 맨 위에 로드 순서와 의존 관계가 적혀 있습니다.

자세한 구조와 게임 설계 메모는 [README.md](README.md)를 보세요.

## 커밋 전에 반드시

```bash
node test/game.test.js index.html
```

캔버스·DOM·오디오를 스텁으로 막고 게임 루프를 직접 돌리는 헤드리스 테스트입니다.
난수가 시드 고정이라 결과가 재현됩니다. **스테이지 완주 가능 여부와 보스 격파 가능
여부까지 검사**하므로, 스테이지를 고쳤다면 이게 진짜 안전망입니다.

브라우저 패널이 숨겨져 있으면 `requestAnimationFrame`이 멈춰 눈으로 확인이 안 됩니다.
그럴 땐 콘솔에서 `frame(performance.now() + i*16.7)`을 직접 돌려 프레임을 진행시킬 수 있습니다
(전역이 노출돼 있어 가능합니다).

## 작업 순서 메모

다음에 할 일은 [TODO.md](TODO.md)에 있습니다.
